jQuery-plugins
==============

stretch

jQuery plugin to transform images into resizable / responsive images. 

Usage: 
```javascript
  $("selector").stretch({
	spanWidth: '32%',
	maximumWidth: '100%',
	className: 'wrapped',
	debug: 0
  });
```
